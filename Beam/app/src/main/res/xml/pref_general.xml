<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

     <SwitchPreference
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:defaultValue="true"
        android:key="switch_preference_1"
        android:summary="@string/pref_description_beam_visibility"
        android:title="@string/pref_title_beam_visibility" />

   <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <SwitchPreference
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:defaultValue="true"
        android:key="switch_preference_2"
        android:summary="@string/pref_description_remember_login"
        android:title="@string/pref_title_remember_login" />


    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_nickname"
        android:inputType="textCapWords"
        android:key="nickname"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_nickname" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

</PreferenceScreen>
